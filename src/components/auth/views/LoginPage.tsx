import { AuthLayout } from '../../layouts';
import { FormLogin } from '../components/FormLogin';

export const LoginPage = () => {
  return (
    <AuthLayout>
      <FormLogin />
    </AuthLayout>
  );
};
